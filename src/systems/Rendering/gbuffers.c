// GBuffer implementation removed when reverting to single-attachment Framebuffer.
// Left intentionally empty to avoid build errors while the engine uses single
// framebuffer objects (createFramebuffer, resizeFramebuffer, bindFramebuffer,
// unbindFramebuffer, destroyFramebuffer).
